export type RegisterForm = {
    email: string
    password: string
    firstName: string
    lastName: string
    department: string
}

export type LoginForm = {
    email: string
    password: string
}